(function($) {
  if ($ === undefined) {
    if (console && console.error) {
      console.error("need Zepto or jQuery to do AJAX calls");
    }

    return;
  }

  console.log("TODO: Add logic here for providing a simple nestable db api");
})(window.Zepto || window.jQuery);